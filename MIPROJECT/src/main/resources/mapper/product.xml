<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.etoile.app.dao.ProductMapper">
	<insert id="productInsert">
		insert into product(product_id, product_name,
		product_type, product_image, product_rental) values
		(p_seq, #{productName}, #{productType}, #{productImage}, #{productRental})
	</insert>

</mapper>